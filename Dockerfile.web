FROM node:22-alpine

WORKDIR /app

ENV CI=true

RUN corepack enable && corepack prepare pnpm@9.15.4 --activate

COPY . .

RUN pnpm install --frozen-lockfile

RUN pnpm --filter @gcr/web build

EXPOSE 3000

CMD ["sh", "-c", "pnpm --filter @gcr/web run preview --host 0.0.0.0 --port ${PORT:-3000}"]
